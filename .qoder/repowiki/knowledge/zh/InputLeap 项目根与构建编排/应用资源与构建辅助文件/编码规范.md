- SVG 图标按语义状态命名（如 input-leap-connected.svg / input-leap-disconnected.svg / input-leap-transfering.svg），并为每种状态配套提供同名 -mask.svg 变体。
- Linux 桌面集成文件采用 org.freedesktop 反域名 ID 前缀（`io.github.input_leap.*`），保持与包管理器一致的应用标识。